function sendSensorInfoIR1() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR1Am',
        'SensorOB': 'SensorIR1Ob'

    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoIR2() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR2Am',
        'SensorOB': 'SensorIR2Ob'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoIR3() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR3Am',
        'SensorOB': 'SensorIR3Ob'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoIR4() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR4Am',
        'SensorOB': 'SensorIR4Ob'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoIR5() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR5Am',
        'SensorOB': 'SensorIR5Ob'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoIR6() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR6Am',
        'SensorOB': 'SensorIR6Ob'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoIR7() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR7Am',
        'SensorOB': 'SensorIR7Ob'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoIR8() {
    let sensorInfo = {
        'SensorDS': 'null',
        'SensorAM': 'SensorIR8Am',
        'SensorOB': 'SensorIR8Ob'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoDS1() {
    let sensorInfo = {
        'SensorDS': 'SensorDS1'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}

function sendSensorInfoDS2() {
    let sensorInfo = {
        'SensorDS': 'SensorDS2'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}
function sendSensorInfoDS3() {
    let sensorInfo = {
        'SensorDS': 'SensorDS3'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}
function sendSensorInfoDS4() {
    let sensorInfo = {
        'SensorDS': 'SensorDS4'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}
function sendSensorInfoDS5() {
    let sensorInfo = {
        'SensorDS': 'SensorDS5'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}
function sendSensorInfoDS6() {
    let sensorInfo = {
        'SensorDS': 'SensorDS6'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}
function sendSensorInfoDS7() {
    let sensorInfo = {
        'SensorDS': 'SensorDS7'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}
function sendSensorInfoDS8() {
    let sensorInfo = {
        'SensorDS': 'SensorDS8'
    }
    const request = new XMLHttpRequest()
    request.open('POST', `/processSensorInfo/${JSON.stringify(sensorInfo)}`)
    request.onload = () => {
        const flaskMessage = request.responseText
    }
    request.send()
}
